 @using Events.IO.Application.ViewModels
@model EventViewModel
@{
    var states = (IEnumerable<SelectListItem>)new AddressViewModel().States();
}
<select asp-for="Address.State" asp-items="@states" class="form-control">

</select>
 